import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {UserProperty} from './user-property';

describe('UserProperty', () => {
  it('should create an instance', () => {
    expect(new UserProperty()).toBeTruthy();
  });
});
