import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {PairwiseCompare} from './pairwise-compare';

describe('PairwiseCompare', () => {
  it('should create an instance', () => {
    expect(new PairwiseCompare()).toBeTruthy();
  });
});
