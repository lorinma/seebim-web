import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {Clause} from './clause';

describe('Clause', () => {
  it('should create an instance', () => {
    expect(new Clause()).toBeTruthy();
  });
});
