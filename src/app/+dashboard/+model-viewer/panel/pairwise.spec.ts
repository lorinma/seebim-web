import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {Pairwise} from './pairwise';

describe('Pairwise', () => {
  it('should create an instance', () => {
    expect(new Pairwise()).toBeTruthy();
  });
});
