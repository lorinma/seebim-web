import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {PropertySet} from './property-set';

describe('PropertySet', () => {
  it('should create an instance', () => {
    expect(new PropertySet()).toBeTruthy();
  });
});
