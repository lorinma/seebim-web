import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {UserPropertySet} from './user-property-set';

describe('UserPropertySet', () => {
  it('should create an instance', () => {
    expect(new UserPropertySet()).toBeTruthy();
  });
});
