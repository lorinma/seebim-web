import {
  describe,
  ddescribe,
  expect,
  iit,
  it
} from '@angular/core/testing';
import {TrimbleFile} from './trimble-file';

describe('TrimbleFile', () => {
  it('should create an instance', () => {
    expect(new TrimbleFile()).toBeTruthy();
  });
});
